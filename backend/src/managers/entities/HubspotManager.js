const { ModuleManager } = require('@friggframework/api-module-salesforce');

class HubspotManager extends ModuleManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = HubspotManager;
