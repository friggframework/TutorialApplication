const { ModuleManager } = require('@friggframework/api-module-hubspot');

class HubspotManager extends ModuleManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = HubspotManager;
